# Travel Article App

### Requirements
This project is setup with vite.
Vite requires Node.js version 18+ or 20+. However, some templates require a higher Node.js version to work, please upgrade if your package manager warns about it.

### Running Locally
- `npm install`
- `npm run dev`

